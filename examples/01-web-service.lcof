# Configures an HTTP web service and demonstrates typed fields and an environment alias.
env WEB_PORT: number = $env:WEB_PORT ?? "8080"

block web_service {
    host: string = "0.0.0.0"
    port: number = $env:WEB_PORT
    workers: number = 4
}
